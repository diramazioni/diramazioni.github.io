[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown
markdown_header_adjustment = 4

[fields.sort_key]
label = Sort order
type = sort_key
default = 0

[children]
order_by = sort_key
