[model]
name = media attachments
label = {{ this.name }}

[fields.caption]
label = Caption
type = markdown

[fields.description]
label = Description
type = markdown

[fields.sort_key]
label = Sort order
type = sort_key
default = 0
